services:
  assetserver:
    build: .
    image: assetserver:latest
    runtime: nvidia
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      # Data directories (download on host, mount into container).
      - ./data:/app/data
      # SAM3D model checkpoints (requires HuggingFace auth).
      - ./checkpoints:/app/checkpoints
      # Generated scene outputs.
      - ./outputs:/app/outputs
    ports:
      - "7010:7010"
    command:
      - asset-acquisition-server
      - --config
      - config/server.yaml
      - --host
      - 0.0.0.0
      - --port
      - "7010"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    stdin_open: true
    tty: true
